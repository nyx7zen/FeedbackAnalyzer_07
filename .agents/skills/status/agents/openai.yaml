interface:
  display_name: "Project Status"
  short_description: "Show branch, TODO, and verification status."
  default_prompt: "Use $status to show current Feedback Analyzer progress."
policy:
  allow_implicit_invocation: true
